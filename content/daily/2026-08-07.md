1\. 登录页滚动修复收尾验证  

\- 在 Chrome / Edge / Firefox 桌面端（1366×768、1920×1080）以及 768p 笔记本上验证 \`disableScroll\` \+ 等比缩小的效果，确认三种登录 tab 切换后滚动条均未出现。  

\- 在移动端（iPhone SE、Pixel 5 模拟器）检查 \`position:fixed\` 锁定视口是否影响键盘弹出时的布局，确认卡片内容可滚动且无异常偏移。  

\- 补充备注：\`pages\.json\` 中 \`disableScroll: true\` 在 H5 生效，但在 App 端需配合 \`position:fixed\` 双保险，已记录到项目文档。

2\. 日期选择器一致性优化  

\- 将请假模块 \`detail\.vue\` 中未改的日期选择器也统一替换为 \`wd\-cell\` 默认触发方式，避免其他页面出现类似“关不掉”的问题。  

\- 检查 \`base\.css\` 中居中样式是否覆盖所有使用场景，微调选择器权重（\`\.wd\-picker\_\_popup \.wd\-picker\_\_view\`）确保不破坏 popup 关闭生命周期。  

\- 在 H5 和微信小程序开发者工具中分别验证弹窗打开/选择/关闭均正常。

3\. 技术学习：uni\-app 渲染机制与 Shadow DOM  

\- 深入阅读 uni\-app H5 端 \`uni\-page\` 自定义元素的 Shadow DOM 结构，理解为何外部 CSS/JS 无法穿透控制滚动行为。  

\- 总结出“内容适配优于容器控制”的设计原则，并整理成简短笔记，补充到团队知识库（\`docs/uni\-app\-h5\-滚动最佳实践\.md\`）。  

\- 复习 Wot Design Uni 的 popup 源码，理解 \`\!important\` 与组件内部 \`v\-show\` 切换的冲突原理，避免后续同类问题。

4\. 代码清理与小优化  

\- 移除 \`login/index\.vue\` 中已废弃的 mock 提示相关注释代码。  

\- 将海报模块 \`ShareSheet\.vue\` 中的硬编码颜色值抽离为 \`features/share/theme\.ts\` 常量，便于后续扩展风格。

 二、遇到的问题

\- 微信小程序端 \`wd\-datetime\-picker\` 弹出后无法自动聚焦到滚动选择器，需用户手动滑动，暂无影响，记录为体验优化点。  

\- 无阻塞性问题。

 三、后续计划

\- 跟进后端接口联调（如有），配合测试环境进行请假流程全链路验证。  

\- 处理其他模块（如通知、成绩）的桌面端适配遗留问题。  

\- 将本周积累的 UI 适配经验整理成周报，分享给团队成员。

 四、备注

今日以学习沉淀和细节打磨为主，所有改动均通过本地测试，未引入新 bug。

